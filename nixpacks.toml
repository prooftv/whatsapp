# Nixpacks configuration for Railway
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
cmds = ["npm ci --no-cache --prefer-offline"]

[phases.build]
cmds = ["echo 'Build complete - no build step needed'"]

[start]
cmd = "node src/production-server.js"